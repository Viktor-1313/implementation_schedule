# Настройка поддержания активности сервиса на Render.com

## Проблема
На бесплатном тарифе Render.com сервисы "засыпают" после 15 минут бездействия. При первом запросе после "сна" сервис просыпается, что занимает 20-30 секунд (долгая загрузка).

## Решение
Автоматические запросы каждые 10-14 минут к эндпоинту `/health` не дают сервису заснуть.

---

## ✅ Вариант 1: Render.com Scheduled Jobs (САМЫЙ НАДЕЖНЫЙ) ⭐

### Преимущества:
- ✅ **Встроенная функция Render.com** — работает на той же платформе, что и ваш сервис
- ✅ **Бесплатно** на бесплатном тарифе
- ✅ **Максимальная надежность** — не зависит от внешних сервисов
- ✅ **Простая настройка** через веб-интерфейс Render
- ✅ **Работает даже для приватных репозиториев**

### Настройка:

1. **Откройте панель Render.com:**
   - Войдите в ваш аккаунт на [render.com](https://render.com)
   - Найдите ваш Web Service

2. **Создайте новый Scheduled Job:**
   - В левом меню выберите **"New"** → **"Scheduled Job"**
   - Или перейдите в раздел **"Scheduled Jobs"** и нажмите **"New Scheduled Job"**

3. **Настройте Job:**
   - **Name:** `Keep Service Alive`
   - **Schedule:** `*/10 * * * *` (каждые 10 минут)
   - **Command:** `node keep-alive.js`
   - **Environment:** выберите тот же environment, что и у вашего Web Service
   - **Working Directory:** оставьте пустым (будет использоваться корень репозитория)

4. **Добавьте переменную окружения (опционально):**
   - Если ваш URL отличается от `https://icona-design.onrender.com`, добавьте:
     - **Key:** `RENDER_SERVICE_URL`
     - **Value:** `https://ваш-проект.onrender.com`

5. **Сохраните и активируйте:**
   - Нажмите **"Create Scheduled Job"**
   - Job будет запускаться автоматически каждые 10 минут

6. **Проверьте работу:**
   - Перейдите в раздел **"Logs"** вашего Scheduled Job
   - Должны появляться сообщения об успешных запросах каждые 10 минут
   - В логах Web Service должны появляться запросы к `/health`

### Как это работает:
- Render.com Scheduled Job запускается по расписанию (каждые 10 минут)
- Выполняет скрипт `keep-alive.js`, который отправляет HTTP-запрос на `/health`
- Сервис получает запрос и не засыпает
- Сайт всегда быстро отвечает

### Файл скрипта:
Скрипт `keep-alive.js` уже создан в папке `1/`. Он автоматически отправляет запрос к эндпоинту `/health` вашего сервиса.

---

## ✅ Вариант 2: GitHub Actions

### Преимущества:
- ✅ Бесплатно
- ✅ Работает автоматически при каждом push
- ✅ Не требует внешних сервисов
- ✅ Простая настройка

### Недостатки:
- ⚠️ Может не работать для приватных репозиториев на бесплатном тарифе GitHub
- ⚠️ Зависит от доступности GitHub Actions

### Настройка:

1. **Откройте файл** `.github/workflows/keep-alive.yml`

2. **URL уже настроен** в файле `.github/workflows/keep-alive.yml`:
   ```yaml
   curl -f https://icona-design.onrender.com/health
   ```
   
   Если нужно изменить URL, отредактируйте файл.

3. **Закоммитьте и запушьте** в GitHub:
   ```bash
   git add .github/workflows/keep-alive.yml
   git commit -m "Add keep-alive workflow"
   git push
   ```

4. **Проверьте работу:**
   - Откройте ваш репозиторий на GitHub
   - Перейдите в раздел "Actions"
   - Должен появиться workflow "Keep Render Service Alive"
   - Он будет запускаться автоматически каждые 10 минут

### Как это работает:
- GitHub Actions запускается по расписанию (каждые 10 минут)
- Отправляет HTTP-запрос на `/health`
- Сервис получает запрос и не засыпает
- Сайт всегда быстро отвечает

---

## ✅ Вариант 3: cron-job.org (АЛЬТЕРНАТИВА)

### Преимущества:
- ✅ Бесплатно
- ✅ Не зависит от GitHub
- ✅ Простая настройка через веб-интерфейс

### Настройка:

1. **Зарегистрируйтесь** на [cron-job.org](https://cron-job.org) (бесплатно)

2. **Создайте новую задачу:**
   - **URL:** `https://icona-design.onrender.com/health`
   - **Расписание:** Каждые 10 минут (`*/10 * * * *`)
   - **Метод:** GET
   - **Название:** "Keep Render Alive"

3. **Сохраните** и активируйте задачу

4. **Проверьте работу:**
   - В логах cron-job.org должны появиться успешные запросы
   - Ваш сервис на Render не будет засыпать

---

## ✅ Вариант 4: UptimeRobot (АЛЬТЕРНАТИВА)

### Преимущества:
- ✅ Бесплатно (50 мониторов)
- ✅ Дополнительно: мониторинг доступности сайта
- ✅ Уведомления при недоступности

### Настройка:

1. **Зарегистрируйтесь** на [UptimeRobot.com](https://uptimerobot.com) (бесплатно)

2. **Создайте новый монитор:**
   - **Тип:** HTTP(s)
   - **URL:** `https://icona-design.onrender.com/health`
   - **Интервал:** 5 минут (минимальный бесплатный)
   - **Название:** "Render Keep Alive"

3. **Сохраните** монитор

4. **Готово!** Монитор будет проверять сайт каждые 5 минут

---

## 🔍 Проверка работы

### Проверьте эндпоинт вручную:
Откройте в браузере:
```
https://icona-design.onrender.com/health
```

Должен вернуться JSON:
```json
{
  "status": "ok",
  "timestamp": "2025-01-15T12:00:00.000Z",
  "uptime": 3600
}
```

### Проверьте логи на Render:
1. Откройте панель Render.com
2. Перейдите в раздел "Logs"
3. Должны появляться запросы к `/health` каждые 10 минут

---

## ⚙️ Настройка интервала

### Render.com Scheduled Jobs:
В настройках Scheduled Job измените расписание:
- `*/10 * * * *` — каждые 10 минут (рекомендуется)
- `*/14 * * * *` — каждые 14 минут (максимум перед сном)

### GitHub Actions:
Измените в `.github/workflows/keep-alive.yml`:
```yaml
- cron: '*/10 * * * *'  # Каждые 10 минут
- cron: '*/14 * * * *'  # Каждые 14 минут (максимум перед сном)
```

### cron-job.org:
В настройках задачи измените расписание на `*/14 * * * *`

### UptimeRobot:
В настройках монитора выберите интервал 5 минут (минимальный)

---

## 📝 Примечания

- **Интервал 10 минут** — оптимальный баланс между частотой запросов и расходом ресурсов
- **Интервал 14 минут** — максимальный перед тем, как сервис заснет (15 минут)
- Эндпоинт `/health` не нагружает сервер, просто возвращает статус
- Все варианты бесплатны и не требуют дополнительных настроек

---

## 🎯 Рекомендация

**Используйте Render.com Scheduled Jobs** — это самый надежный вариант, так как:
- ✅ Работает на той же платформе, что и ваш сервис
- ✅ Не зависит от внешних сервисов (GitHub, cron-job.org и т.д.)
- ✅ Работает даже для приватных репозиториев
- ✅ Максимальная надежность и скорость отклика
- ✅ Простая настройка через веб-интерфейс Render

**Если Render.com Scheduled Jobs недоступен**, используйте **cron-job.org** как альтернативу — это самый простой внешний сервис для настройки.

