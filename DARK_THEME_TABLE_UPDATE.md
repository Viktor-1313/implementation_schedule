# Обновление темной темы для таблицы задач

## Что было сделано

Добавлена полная поддержка темной темы для детальной таблицы задач (`.tasks-table`).

### Добавленные стили темной темы

#### 0. Контейнер таблицы
- ✅ `.table-section` - темный фон контейнера таблицы
- ✅ `.table-section h2` - голубой заголовок "Детальная таблица задач"
- ✅ `.table-section.view-active` - подсветка активного контейнера

#### 1. Основная таблица
- ✅ `.tasks-table` - темный фон таблицы
- ✅ `.tasks-table th` - голубой фон заголовков с белым текстом
- ✅ `#tasksTable thead th` - специфичные стили для заголовка с тенью
- ✅ `.tasks-table td` - темный фон ячеек с светлым текстом

#### 2. Интерактивные состояния строк
- ✅ `.tasks-table tr:hover` - подсветка при наведении
- ✅ `.tasks-table tr:nth-child(even)` - темный фон для четных строк
- ✅ `.tasks-table tr.selected` - голубая подсветка выделенных строк
- ✅ `.tasks-table tr.selected td` - правильный цвет текста в выделенных строках

#### 3. Поля ввода и выпадающие списки
- ✅ `.tasks-table-input` - темный фон полей ввода
- ✅ `.tasks-table-input-select` - темный фон выпадающих списков
- ✅ `.tasks-table-input:focus` - подсветка при фокусе
- ✅ `.tasks-table-input[readonly]` - неактивные поля в режиме просмотра
- ✅ `.tasks-table-input[disabled]` - заблокированные поля

#### 4. Обертка таблицы и scrollbar
- ✅ `.tasks-table-wrapper` - темный фон обертки
- ✅ `.tasks-table-wrapper::-webkit-scrollbar` - темный scrollbar
- ✅ `.tasks-table-wrapper::-webkit-scrollbar-thumb` - голубой ползунок scrollbar
- ✅ `scrollbar-color` для Firefox

#### 5. Режим просмотра (view-mode)
- ✅ `body.view-mode .tasks-table-input[readonly]` - неактивные поля
- ✅ `body.view-mode .tasks-table-input[data-field=*]` - все типы полей
- ✅ `body.view-mode .tasks-table-input-select[disabled]` - заблокированные select

## Результат

Теперь детальная таблица задач полностью поддерживает темную тему и выглядит единообразно с диаграммой Ганта:

- 🌙 Темный фон таблицы (`#1e1e1e`)
- 🔵 Голубые заголовки (`#42a5f5`)
- ⚪ Светлый текст (`#e0e0e0`)
- 🎨 Правильные цвета для всех интерактивных элементов
- 📜 Темный scrollbar с голубым ползунком
- 👁️ Корректное отображение в режиме просмотра

## Использование

Темная тема автоматически применяется при переключении темы через кнопку в правом верхнем углу страницы. Все элементы таблицы задач теперь правильно окрашиваются в соответствии с выбранной темой.

