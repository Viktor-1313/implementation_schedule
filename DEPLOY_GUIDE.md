# 🚀 Пошаговая инструкция по деплою в интернет

## Выбор платформы (бесплатные варианты)

Для вашего проекта нужен сервер с **динамическим сохранением файлов** (не статичный хостинг). Рекомендуемые бесплатные платформы:

1. **Render.com** ⭐ (рекомендуется) - бесплатный тариф, постоянное хранилище
2. **Railway.app** - бесплатный тариф с кредитами ($5/месяц)
3. **Fly.io** - бесплатный тариф

---

## 📋 Подготовка к деплою

### Шаг 1: Подготовьте файлы проекта

Убедитесь, что в папке `1` есть все необходимые файлы:

```
1/
├── server.js              ✅ Сервер Node.js
├── package.json           ✅ Зависимости
├── auth.html              ✅ Страница авторизации
├── companies.html          ✅ Страница выбора компаний
├── admin.html             ✅ Админ-панель
├── implementation_schedule.html  ✅ График Ганта
├── index.html             ✅ Редирект на авторизацию
├── xlsx.min.js            ✅ Библиотека для Excel
├── favicon.png            ✅ Иконка
└── logo.png               ✅ Логотип (опционально)
```

### Шаг 2: Проверьте package.json

Убедитесь, что в `package.json` есть:
- Скрипт `start`: `"start": "node server.js"`
- Зависимости: `express`, `cors`, `bcrypt`
- Версия Node.js: `"engines": { "node": ">=14.0.0" }`

---

## 🌐 Вариант 1: Render.com (РЕКОМЕНДУЕТСЯ)

### Преимущества:
- ✅ Бесплатный тариф
- ✅ Постоянное хранилище файлов
- ✅ Автоматический деплой из GitHub
- ✅ HTTPS из коробки
- ✅ Простая настройка

### Пошаговая инструкция:

#### 1. Зарегистрируйтесь на Render.com
- Перейдите на https://render.com
- Зарегистрируйтесь через GitHub (удобнее всего)

#### 2. Подготовьте репозиторий GitHub
- Создайте репозиторий на GitHub (если еще нет)
- Загрузите папку `1` в репозиторий
- Или создайте репозиторий только с содержимым папки `1`

#### 3. Создайте Web Service на Render
1. В Dashboard нажмите **"New +"** → **"Web Service"**
2. Подключите ваш GitHub репозиторий
3. Выберите репозиторий и ветку (обычно `main` или `master`)

#### 4. Настройте параметры деплоя:
```
Name: icona-gantt (или любое имя)
Region: Frankfurt (или ближайший к вам)
Branch: main
Root Directory: (оставьте пустым, если файлы в корне репозитория)
Runtime: Node
Build Command: npm install
Start Command: npm start
Instance Type: Free
```

#### 5. Настройте переменные окружения (опционально):
```
PORT=3001
MAIN_ADMIN_PASSWORD=ваш_секретный_пароль (опционально)
```

#### 6. Нажмите **"Create Web Service"**

#### 7. Дождитесь деплоя
- Render автоматически установит зависимости и запустит сервер
- Процесс займет 2-5 минут
- После успешного деплоя вы получите URL вида: `https://icona-gantt.onrender.com`

#### 8. Проверьте работу
- Откройте URL в браузере
- Должна открыться страница авторизации
- Проверьте создание пользователя через админ-панель

### ⚠️ Важные замечания для Render:
- **Бесплатный тариф**: сервер "засыпает" после 15 минут неактивности, первая загрузка может быть медленной
- **Файлы сохраняются**: данные сохраняются в файловой системе контейнера
- **Автоматический деплой**: при каждом push в GitHub происходит автоматический деплой

---

## 🚂 Вариант 2: Railway.app

### Преимущества:
- ✅ Бесплатный тариф ($5 кредитов/месяц)
- ✅ Постоянное хранилище
- ✅ Быстрый деплой
- ✅ Автоматический HTTPS

### Пошаговая инструкция:

#### 1. Зарегистрируйтесь на Railway.app
- Перейдите на https://railway.app
- Зарегистрируйтесь через GitHub

#### 2. Создайте новый проект
1. Нажмите **"New Project"**
2. Выберите **"Deploy from GitHub repo"**
3. Выберите ваш репозиторий

#### 3. Railway автоматически определит Node.js проект
- Автоматически найдет `package.json`
- Установит зависимости
- Запустит `npm start`

#### 4. Настройте переменные окружения (опционально):
В настройках проекта → Variables:
```
PORT=3001
MAIN_ADMIN_PASSWORD=ваш_секретный_пароль
```

#### 5. Получите URL
- Railway автоматически создаст домен вида: `your-project.up.railway.app`
- Можно настроить кастомный домен в настройках

#### 6. Проверьте работу
- Откройте URL в браузере
- Должна открыться страница авторизации

---

## ✈️ Вариант 3: Fly.io

### Преимущества:
- ✅ Бесплатный тариф
- ✅ Глобальная сеть
- ✅ Быстрая работа

### Пошаговая инструкция:

#### 1. Установите Fly CLI
```bash
# Windows (PowerShell)
iwr https://fly.io/install.ps1 -useb | iex

# Mac/Linux
curl -L https://fly.io/install.sh | sh
```

#### 2. Зарегистрируйтесь
```bash
fly auth signup
```

#### 3. Создайте приложение
В папке `1` выполните:
```bash
fly launch
```

#### 4. Следуйте инструкциям:
- Выберите регион
- Назовите приложение
- Fly создаст `fly.toml` автоматически

#### 5. Деплой
```bash
fly deploy
```

#### 6. Получите URL
```bash
fly open
```

---

## 🔧 Настройка после деплоя

### 1. Создайте главного администратора

После первого запуска сервер автоматически создаст главного администратора:
- **Логин**: `Driga_VA`
- **Пароль**: `Admin2024!` (или значение из переменной окружения `MAIN_ADMIN_PASSWORD`)

### 2. Войдите в админ-панель

1. Откройте: `https://ваш-домен.com/admin.html`
2. Войдите с логином `Driga_VA` и паролем
3. Создайте компании и пользователей

### 3. Проверьте сохранение данных

1. Создайте компанию через админ-панель или страницу выбора компаний
2. Откройте график компании
3. Внесите изменения (добавьте задачу, измените даты)
4. Обновите страницу - данные должны сохраниться

---

## 📝 Создание ссылок для пользователей

### Для просмотра конкретной компании:

```
https://ваш-домен.com/auth.html?company=SetlTech
```

Пользователь:
1. Переходит по ссылке
2. Видит экран авторизации
3. Нажимает "Режим просмотра"
4. Попадает на график компании `SetlTech` в режиме просмотра

### Для авторизованных пользователей:

```
https://ваш-домен.com/auth.html
```

После авторизации пользователь попадает на страницу выбора компаний.

---

## 🔒 Безопасность

### Рекомендации:

1. **Измените пароль главного администратора**:
   - Установите переменную окружения `MAIN_ADMIN_PASSWORD` на сервере
   - Или измените пароль через админ-панель после первого входа

2. **Используйте HTTPS**: Все платформы предоставляют HTTPS автоматически

3. **Ограничьте доступ к админ-панели** (опционально):
   - Можно добавить базовую HTTP-аутентификацию на уровне веб-сервера
   - Или ограничить доступ по IP-адресам

---

## 🐛 Решение проблем

### Проблема: Сервер не запускается

**Решение:**
- Проверьте логи в панели управления платформы
- Убедитесь, что `package.json` содержит все зависимости
- Проверьте, что `server.js` находится в корне проекта

### Проблема: Данные не сохраняются

**Решение:**
- Убедитесь, что используется платформа с постоянным хранилищем (Render, Railway, Fly.io)
- Проверьте права на запись файлов
- Проверьте логи сервера на ошибки

### Проблема: Медленная загрузка (Render бесплатный тариф)

**Решение:**
- Это нормально для бесплатного тарифа Render
- Сервер "просыпается" после первого запроса
- Рассмотрите платный тариф для продакшена

### Проблема: Порт не указан

**Решение:**
- Платформы автоматически устанавливают переменную `PORT`
- Сервер использует `process.env.PORT || 3001`
- Ничего менять не нужно

---

## 📊 Мониторинг и логи

### Render.com:
- Логи доступны в разделе "Logs" в Dashboard
- Можно видеть все запросы и ошибки

### Railway.app:
- Логи доступны в разделе "Deployments" → выберите деплой → "View Logs"

### Fly.io:
```bash
fly logs
```

---

## 🎯 Итоговый чеклист

После деплоя проверьте:

- [ ] Страница авторизации открывается по корневому URL
- [ ] Можно войти как главный администратор (`Driga_VA`)
- [ ] Админ-панель доступна и работает
- [ ] Можно создать компанию
- [ ] Можно открыть график компании
- [ ] Изменения в графике сохраняются
- [ ] Загрузка логотипа компании работает
- [ ] Ссылка `auth.html?company=xxx` работает для просмотра
- [ ] Данные сохраняются между перезагрузками страницы

---

## 💡 Полезные ссылки

- **Render.com**: https://render.com/docs
- **Railway.app**: https://docs.railway.app
- **Fly.io**: https://fly.io/docs

---

## 📞 Поддержка

Если возникли проблемы:
1. Проверьте логи сервера
2. Убедитесь, что все файлы загружены в репозиторий
3. Проверьте переменные окружения
4. Убедитесь, что порт настроен правильно









