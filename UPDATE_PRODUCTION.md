# 🚀 Как обновить код на продакшене (Render.com)

## Быстрый способ (автоматический деплой)

Если ваш репозиторий подключен к Render.com, обновление происходит автоматически:

### 1. Закоммитьте изменения в Git

```bash
# Перейдите в папку с проектом
cd путь/к/вашему/проекту

# Проверьте статус изменений
git status

# Добавьте все измененные файлы
git add .

# Создайте коммит с описанием изменений
git commit -m "Обновление: перемещена кнопка Гант/Таблица, улучшено сохранение данных"

# Отправьте изменения на GitHub
git push origin main
```

### 2. Render.com автоматически задеплоит изменения

- Render.com отслеживает изменения в репозитории
- При каждом `git push` запускается автоматический деплой
- Процесс занимает 2-5 минут
- Вы получите уведомление о статусе деплоя

### 3. Проверьте статус деплоя

1. Откройте https://render.com
2. Войдите в ваш аккаунт
3. Откройте ваш Web Service
4. Перейдите на вкладку **"Events"** или **"Logs"**
5. Увидите процесс деплоя в реальном времени

## Ручной деплой (если автоматический не работает)

### 1. В панели Render.com

1. Откройте ваш Web Service
2. Нажмите кнопку **"Manual Deploy"** → **"Deploy latest commit"**
3. Дождитесь завершения деплоя

## Проверка после обновления

### 1. Проверьте работу сайта

1. Откройте ваш URL: `https://ваш-домен.onrender.com`
2. Войдите в систему
3. Откройте график
4. Проверьте, что кнопка "Гант/Таблица" теперь первая
5. Проверьте сохранение данных (измените дату, обновите страницу)

### 2. Проверьте логи (если что-то не работает)

1. В Render.com откройте ваш сервис
2. Перейдите на вкладку **"Logs"**
3. Найдите ошибки (они будут выделены красным)
4. Скопируйте текст ошибки для диагностики

## Что было обновлено в этом коммите

1. ✅ Кнопка "Гант/Таблица" перемещена на первое место
2. ✅ Улучшено логирование сохранения данных
3. ✅ Добавлено принудительное сохранение при уходе со страницы
4. ✅ Улучшена обработка ошибок при сохранении

## Если что-то пошло не так

### Откат к предыдущей версии

1. В Render.com откройте ваш сервис
2. Перейдите на вкладку **"Events"**
3. Найдите предыдущий успешный деплой
4. Нажмите на три точки → **"Rollback to this deploy"**

### Проверка изменений перед деплоем

```bash
# Посмотреть, что изменилось
git diff

# Посмотреть историю коммитов
git log --oneline -10
```

## Полезные команды Git

```bash
# Проверить статус
git status

# Посмотреть изменения в файле
git diff путь/к/файлу

# Отменить изменения в файле (если еще не закоммитили)
git checkout -- путь/к/файлу

# Посмотреть историю
git log --oneline --graph -10
```

## Рекомендации

1. **Всегда делайте коммиты с понятными сообщениями** - это поможет понять, что изменилось
2. **Проверяйте работу локально** перед отправкой на продакшен
3. **Следите за логами** после деплоя - там могут быть предупреждения
4. **Делайте бэкапы** важных данных перед большими изменениями

## Структура процесса обновления

```
Локальные изменения
    ↓
git add .
    ↓
git commit -m "описание"
    ↓
git push origin main
    ↓
GitHub получает изменения
    ↓
Render.com видит изменения
    ↓
Автоматический деплой запускается
    ↓
Сборка проекта (npm install)
    ↓
Запуск сервера (npm start)
    ↓
Проверка работы
    ↓
Готово! ✅
```

## Время обновления

- **Автоматический деплой:** 2-5 минут
- **Ручной деплой:** 1-3 минуты
- **Первый деплой:** 5-10 минут (установка зависимостей)

---

**Готово!** После `git push` ваши изменения автоматически появятся на продакшене через несколько минут.


