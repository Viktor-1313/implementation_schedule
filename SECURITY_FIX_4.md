# Исправление критической проблемы #4: XSS уязвимости (в процессе)

**Дата:** 2025-01-XX  
**Статус:** 🔄 В процессе

## Проблема

Использование `innerHTML` без санитизации данных создавало серьезные XSS уязвимости:
- Пользовательские данные (имена компаний, имена пользователей) вставлялись напрямую в HTML
- Злоумышленник мог внедрить JavaScript код через названия компаний или имена пользователей
- 65+ мест использования `innerHTML` в коде

## Решение

### 1. Созданы функции для безопасной работы с HTML

Добавлены функции во все HTML файлы:

#### `escapeHtml(text)` - Экранирование HTML
```javascript
function escapeHtml(text) {
    if (text == null) return '';
    const div = document.createElement('div');
    div.textContent = text;
    return div.innerHTML;
}
```
- Экранирует все специальные HTML символы (`<`, `>`, `&`, `"`, `'`)
- Безопасно для вставки пользовательских данных в HTML

#### `setSafeHtml(element, html)` - Безопасная вставка статического HTML
- Используется только для статического HTML без пользовательских данных
- Алиас для `innerHTML` для ясности кода

#### `setSafeText(element, text)` - Безопасная вставка текста
```javascript
function setSafeText(element, text) {
    if (!element) return;
    element.textContent = text;
}
```
- Использует `textContent` вместо `innerHTML`
- Полностью безопасно, но не поддерживает HTML разметку

### 2. Начата замена опасных использований

#### ✅ Исправлено в `companies.html`:
- Замена `innerHTML` с данными компаний на безопасное экранирование
- Использование `escapeHtml()` для названий компаний, ID, дат

#### ✅ Исправлено в `admin.html`:
- Замена `innerHTML` с данными пользователей на безопасное экранирование
- Использование `escapeHtml()` для имен пользователей, логинов, списков компаний

#### ✅ Добавлено в `implementation_schedule.html`:
- Функции безопасности добавлены
- Требуется замена использований `innerHTML` с пользовательскими данными

## Оставшиеся задачи

### Критичные места для исправления:

#### `implementation_schedule.html` (13 использований):
1. ✅ Функции безопасности добавлены
2. ✅ Строка 12192: `row.innerHTML = ...` - строки таблицы задач (исправлено: экранирование task.responsible)
3. ✅ Строка 12238: `data-tooltip` с task.responsible (исправлено: экранирование)
4. ✅ Строка 12150, 12166: экранирование значений в tooltip (исправлено: используется escapeHtml)
5. ✅ Строка 10508: `data-tooltip` с комментариями (исправлено: экранирование)
6. ✅ Строка 21878: `tooltip.innerHTML = ...` - подсказки тура (исправлено: экранирование step.title и step.text)
7. ✅ Все места с `setAttribute('data-tooltip', ...)` исправлены (экранирование пользовательских данных)
8. ✅ Строка 7104, 9989: статический HTML (безопасно)

#### `companies.html` (15 использований):
1. ✅ Основные места с данными компаний исправлены
2. ⏳ Строка 4117: `container.innerHTML = html` - нужно проверить источник `html`
3. ⏳ Строка 4876: `currentLogoDiv.innerHTML = ...` - логотипы компаний
4. ⏳ Строка 6328: `createSelect.innerHTML = ...` - выпадающие списки
5. ⏳ Строка 8270: `content.innerHTML = html` - статистика (может содержать пользовательские данные)

#### `admin.html` (19 использований):
1. ✅ Основные места с данными пользователей исправлены
2. ⏳ Строка 2939: `thead.innerHTML = ...` - заголовки таблиц
3. ⏳ Строка 2960: `tbody.innerHTML = ...` - строки таблиц скелетов (может содержать пользовательские данные)
4. ⏳ Строка 3691: `tbody.innerHTML = ...` - строки таблиц логов (может содержать пользовательские данные)

## Рекомендации по исправлению

### Для строк таблиц с пользовательскими данными:

**Было:**
```javascript
row.innerHTML = `
    <td>${task.name}</td>
    <td>${task.stage}</td>
`;
```

**Должно быть:**
```javascript
row.innerHTML = `
    <td>${escapeHtml(task.name)}</td>
    <td>${escapeHtml(task.stage)}</td>
`;
```

### Для статического HTML:

**Было:**
```javascript
container.innerHTML = '<div>Статический текст</div>';
```

**Можно оставить как есть** (статические строки безопасны), но лучше использовать:
```javascript
setSafeHtml(container, '<div>Статический текст</div>');
```

### Для текста без HTML:

**Было:**
```javascript
element.innerHTML = user.name;
```

**Должно быть:**
```javascript
setSafeText(element, user.name);
// или
element.textContent = user.name;
```

## Приоритеты исправления

### 🔴 Критично (содержат пользовательские данные):
1. Строки таблиц задач в `implementation_schedule.html` (строка 12158)
2. Статистика компаний в `companies.html` (строка 8270)
3. Таблицы скелетов в `admin.html` (строка 2960)
4. Таблицы логов в `admin.html` (строка 3691)

### 🟠 Высокий приоритет:
1. Подсказки с комментариями в `implementation_schedule.html` (строка 21844)
2. Выпадающие списки с данными компаний (строки 6328, 6336)
3. Логотипы компаний (строка 4876)

### 🟡 Средний приоритет:
1. Статический HTML (можно оставить, но лучше заменить для единообразия)
2. Заголовки таблиц (обычно статические)

## Тестирование

После исправления всех мест нужно протестировать:

1. **Ввод XSS payload в названия компаний:**
   ```
   <script>alert('XSS')</script>
   <img src=x onerror=alert('XSS')>
   ```

2. **Ввод XSS payload в имена пользователей:**
   ```
   <script>alert('XSS')</script>
   ```

3. **Ввод XSS payload в названия задач:**
   ```
   <script>alert('XSS')</script>
   ```

Все эти payload должны быть экранированы и отображаться как текст, а не выполняться как код.

## Дополнительные меры безопасности

После исправления всех XSS уязвимостей можно:

1. **Ужесточить Content-Security-Policy:**
   - Убрать `'unsafe-inline'` для скриптов
   - Использовать nonce или hash для inline скриптов

2. **Использовать библиотеку DOMPurify** для сложных случаев:
   ```javascript
   const DOMPurify = require('dompurify');
   container.innerHTML = DOMPurify.sanitize(userContent);
   ```

3. **Валидация на сервере:**
   - Проверять и санитизировать все пользовательские данные на сервере
   - Ограничить длину полей
   - Запретить специальные символы где возможно

## Прогресс

- ✅ Функции безопасности созданы во всех файлах
- ✅ Основные места в `companies.html` исправлены
- ✅ Основные места в `admin.html` исправлены
- ⏳ Требуется исправление в `implementation_schedule.html`
- ⏳ Требуется проверка всех остальных использований

---

## Исправление ошибок авторизации на dev ветке (2025-01-XX)

### Проблема
На dev ветке возникали ошибки 500 "Ошибка проверки авторизации" при загрузке компаний и типов графиков:
- `GET /api/companies` возвращал 500 вместо данных
- `GET /api/chart-types` возвращал 500 вместо данных
- Клиент падал с `TypeError: companies.map is not a function`

### Причина
API эндпоинты `/api/companies` и `/api/chart-types` использовали `requireAuth` middleware, который требовал обязательной авторизации. Но эти эндпоинты должны быть доступны:
- В режиме просмотра (без авторизации)
- При первой загрузке страницы (до авторизации)

### Решение

#### 1. Изменен middleware для API эндпоинтов
**В `server.js`:**
- ✅ `/api/companies` изменен с `requireAuth` на `optionalAuth` (строка 355)
- ✅ `/api/chart-types` изменен с `requireAuth` на `optionalAuth` (строка 1138)

`optionalAuth` позволяет доступ без авторизации, но загружает данные пользователя, если он авторизован.

#### 2. Улучшена обработка ошибок на клиенте
**В `companies.html`:**
- ✅ Добавлена проверка `response.ok` перед парсингом JSON
- ✅ Добавлена проверка типа данных (`Array.isArray()`) перед использованием `.map()`
- ✅ Улучшены сообщения об ошибках с деталями

**В `admin.html`:**
- ✅ Добавлена проверка `response.ok` для всех API запросов
- ✅ Добавлена проверка типа данных для `companies`, `users`, `chartTypes`
- ✅ Исправлена функция `updateLogCompanyFilter()` - проверка массива перед `.forEach()`
- ✅ Улучшена обработка ошибок в `loadActivityLogs()`

### Результат
- ✅ API эндпоинты работают без авторизации (для режима просмотра)
- ✅ Клиент корректно обрабатывает ошибки и не падает
- ✅ Пользователи видят понятные сообщения об ошибках
- ✅ Режим просмотра работает корректно

### Исправление ошибок авторизации в админ-панели (2025-01-XX)

#### Проблема
В админ-панели возникали ошибки 500 "Ошибка проверки авторизации" для:
- `GET /api/users` - загрузка списка пользователей
- `GET /api/activity-logs` - загрузка логов активности

#### Причина
API запросы в админ-панели отправлялись без заголовков авторизации (`X-User-Login`, `X-User-Name`), которые требуются для `requireAuth` и `requireAdmin` middleware.

#### Решение
**В `admin.html`:**
- ✅ Создана функция `getAuthHeaders()` для получения заголовков авторизации из `localStorage`
- ✅ Добавлены заголовки авторизации для всех API запросов:
  - `loadUsers()` - GET `/api/users`
  - `loadActivityLogs()` - GET `/api/activity-logs`
  - `loadChartTypes()` - GET `/api/chart-types`
  - POST `/api/users` - создание пользователя
  - POST `/api/chart-types` - создание типа графика
  - PUT `/api/users/:login` - обновление пользователя
  - DELETE `/api/users/:login` - удаление пользователя
  - DELETE `/api/chart-types/:id` - удаление типа графика
  - DELETE `/api/activity-logs` - очистка логов

Функция `getAuthHeaders()`:
- Получает данные пользователя из `localStorage.getItem('gantt-user')`
- Добавляет заголовок `X-User-Login` с логином пользователя
- Кодирует имя пользователя в base64 и добавляет заголовки `X-User-Name` и `X-User-Name-Encoded`
- Возвращает объект с заголовками для использования в `fetch()`

### Исправление ошибок для графика и архива (2025-01-XX)

#### Проблема
Возникали ошибки 500 для:
- `GET /api/gantt-skeleton?chartType=icona` - загрузка скелета графика
- `GET /api/companies/archived` - загрузка архивированных компаний

#### Причина
- `/api/gantt-skeleton` требовал `requireAuth`, но должен работать в режиме просмотра
- Запросы не передавали заголовки авторизации

#### Решение
**В `server.js`:**
- ✅ `/api/gantt-skeleton` изменен с `requireAuth` на `optionalAuth` (строка 1045)

**В `companies.html`:**
- ✅ Создана функция `getAuthHeaders()` для получения заголовков авторизации
- ✅ Добавлены заголовки авторизации для запроса `/api/companies/archived`

**В `implementation_schedule.html`:**
- ✅ Добавлены заголовки авторизации для запроса `/api/gantt-skeleton` (используется существующая функция `getAuthHeaders()`)

### Файлы изменены
- `server.js` - изменены middleware для `/api/companies`, `/api/chart-types` и `/api/gantt-skeleton`
- `companies.html` - добавлена функция `getAuthHeaders()` и заголовки авторизации для `/api/companies/archived`
- `implementation_schedule.html` - добавлены заголовки авторизации для `/api/gantt-skeleton`
- `admin.html` - улучшена обработка ошибок и добавлены заголовки авторизации для всех API запросов

### Исправление проблемы с авторизацией после очистки localStorage (2025-01-XX)

#### Проблема
После очистки localStorage и перезагрузки сервера админ-панель не работала:
- Запросы к `/api/users` и `/api/activity-logs` возвращали 500 "Ошибка проверки авторизации"
- Пользователь не перенаправлялся на страницу авторизации при отсутствии данных

#### Причина
- Функции загрузки данных не проверяли наличие данных авторизации перед отправкой запросов
- Ошибки авторизации (401 или "Ошибка проверки авторизации") не обрабатывались для перенаправления на страницу входа

#### Решение
**В `admin.html`:**
- ✅ Добавлена проверка `localStorage.getItem('gantt-user')` перед каждым API запросом
- ✅ При отсутствии данных авторизации - автоматическое перенаправление на `auth.html`
- ✅ Добавлена обработка ошибок авторизации (401 или текст ошибки содержит "авторизац") с перенаправлением
- ✅ Обновлены функции:
  - `loadUsers()` - проверка авторизации и обработка ошибок
  - `loadActivityLogs()` - проверка авторизации и обработка ошибок
  - `loadCompanies()` - добавлены заголовки авторизации, проверка и обработка ошибок
  - `loadChartTypes()` - добавлена обработка ошибок авторизации

Теперь при отсутствии данных авторизации пользователь автоматически перенаправляется на страницу входа, а не видит ошибки 500.

### Исправление ошибки архива компаний (2025-01-XX)

#### Проблема
Запрос к `/api/companies/archived` возвращал ошибку 500 даже после добавления заголовков авторизации.

#### Причина
- Функция `loadArchivedCompanies()` не проверяла авторизацию и роль пользователя перед запросом
- Архив доступен только администраторам, но запрос отправлялся даже для неавторизованных пользователей
- Ошибки авторизации не обрабатывались корректно

#### Решение
**В `companies.html`:**
- ✅ Добавлена проверка наличия данных авторизации перед запросом
- ✅ Добавлена проверка роли пользователя (только админы могут видеть архив)
- ✅ Улучшена обработка ошибок:
  - При отсутствии авторизации - показывается пустой архив
  - При отсутствии прав администратора - показывается пустой архив
  - При ошибках 401/403 - показывается пустой архив без ошибок в консоли
  - Добавлена проверка типа данных (`Array.isArray()`)

Теперь архив корректно работает только для администраторов, а для остальных пользователей просто не отображается без ошибок.

### Исправление проблемы с доступом в админ-панель (2025-01-XX)

#### Проблема
При попытке зайти в админ-панель пользователь автоматически перенаправлялся на страницу авторизации, даже если был авторизован как администратор. Редирект происходил слишком быстро, не давая возможности посмотреть консоль браузера для отладки.

#### Причина
Функция `checkAuth()` в `admin.html` проверяла наличие данных пользователя в `localStorage`, но не выводила достаточно информации для отладки. Возможные причины:
- Данные пользователя не сохранялись правильно при авторизации
- Поле `role` отсутствовало в данных пользователя
- Ошибка парсинга JSON из localStorage
- Редирект происходил мгновенно, не давая времени на просмотр консоли

#### Решение
**В `admin.html`:**
- ✅ Добавлена функция `showAuthError()` для визуального отображения ошибок на странице:
  - Красное модальное окно с описанием проблемы
  - Детальная информация об ошибке
  - Таймер обратного отсчета до редиректа (10 секунд)
- ✅ Улучшена функция `checkAuth()` с подробным логированием:
  - Добавлены console.log для отладки данных пользователя
  - Добавлена проверка наличия поля `role` в данных пользователя
  - Улучшена обработка ошибок с выводом содержимого localStorage при ошибке парсинга
  - Добавлены предупреждения и сообщения об ошибках для каждого этапа проверки
  - Задержка в 10 секунд перед редиректом для просмотра консоли и визуального сообщения
- ✅ Логирование и визуальные сообщения помогают определить точную причину проблемы:
  - Если данных нет - выводится предупреждение "Нет данных пользователя в localStorage" + список всех ключей в localStorage
  - Если роль не определена - выводится ошибка с данными пользователя (логин, имя, роль)
  - Если пользователь не администратор - выводится предупреждение с текущей ролью и требуемой ролью
  - При ошибке парсинга - выводится детальная информация об ошибке и типе данных

Теперь при проблемах с авторизацией:
1. На странице появляется красное модальное окно с описанием проблемы
2. В консоли браузера (F12) видны подробные сообщения
3. Редирект происходит через 10 секунд, давая время на просмотр информации
4. Добавлена проверка данных авторизации сразу при загрузке страницы (inline скрипт в body) для диагностики проблемы с потерей данных при переходе между страницами

**Диагностика:**
- Inline скрипт в `<body>` проверяет данные в localStorage сразу при загрузке страницы
- Основной скрипт проверяет данные при загрузке (до DOMContentLoaded)
- Функция `checkAuth()` проверяет данные при DOMContentLoaded
- Все проверки логируются в консоль с префиксами `[INLINE]`, `🔍`, `✅`, `❌` для удобной отладки

### Исправление синтаксической ошибки (2025-01-XX)

#### Проблема
При попытке зайти в админ-панель возникала ошибка `Uncaught SyntaxError: Unexpected end of input (at admin.html:4837:5)`, которая останавливала выполнение скрипта и приводила к немедленному редиректу на страницу авторизации.

#### Причина
При добавлении задержки перед проверкой авторизации была открыта функция `setTimeout()`, но не была закрыта. Это вызывало синтаксическую ошибку JavaScript, которая останавливала выполнение всего скрипта.

#### Решение
**В `admin.html`:**
- ✅ Исправлена синтаксическая ошибка - добавлена закрывающая скобка для `setTimeout()` на строке 4607
- ✅ Код теперь корректно структурирован:
  ```javascript
  setTimeout(function() {
      // проверка авторизации и загрузка данных
  }, 500); // закрытие setTimeout
  }); // закрытие addEventListener
  ```

Теперь скрипт выполняется полностью, проверка авторизации работает корректно, и пользователи с правами администратора могут зайти в админ-панель.

### Добавлен режим отладки для диагностики проблем с авторизацией (2025-01-XX)

#### Проблема
При проблемах с авторизацией пользователь не мог увидеть детальную информацию в консоли из-за быстрого редиректа.

#### Решение
**В `admin.html`:**
- ✅ Добавлен флаг `window.DEBUG_AUTH` для отключения автоматического редиректа
- ✅ Улучшено логирование при загрузке страницы:
  - Проверка данных сразу при DOMContentLoaded
  - Вывод всех найденных ключей в localStorage
  - Детальная информация о данных пользователя
- ✅ При установке `window.DEBUG_AUTH = true` в консоли:
  - Автоматический редирект отключается
  - Пользователь может спокойно просмотреть консоль и данные
  - Сообщения об ошибках остаются на странице

**Инструкция по использованию:**
1. Откройте админ-панель
2. Откройте консоль браузера (F12)
3. Введите в консоли: `window.DEBUG_AUTH = true`
4. Обновите страницу (F5)
5. Теперь редирект не произойдет, и вы сможете увидеть все сообщения в консоли

Это поможет диагностировать проблемы с авторизацией без необходимости быстро смотреть консоль.

### Улучшение видимости сообщений об ошибках (2025-01-XX)

#### Проблема
Сообщения об ошибках авторизации были слишком маленькими и появлялись ненадолго, пользователь не успевал их увидеть перед редиректом.

#### Решение
**В `admin.html`:**
- ✅ Сообщение об ошибке теперь отображается как полноэкранное модальное окно:
  - Темный фон на весь экран (z-index: 99999)
  - Большое красное окно по центру экрана
  - Крупный текст (18-24px) для лучшей читаемости
  - Инструкция по использованию режима отладки прямо в сообщении
- ✅ Увеличена задержка редиректа до 30 секунд (было 15 секунд)
- ✅ Добавлен таймер обратного отсчета с визуальным предупреждением (желтый цвет при < 10 секунд)
- ✅ Сообщение невозможно пропустить - оно занимает весь экран

Теперь пользователь точно успеет увидеть ошибку и прочитать инструкцию по отладке.

### Исправление проблемы с ошибками 500 при загрузке данных в админ-панели (2025-01-XX)

#### Проблема
После успешной авторизации и проверки на клиенте, при попытке загрузить данные через API (`/api/users`, `/api/activity-logs`) сервер возвращал ошибку 500 "Ошибка проверки авторизации", несмотря на то, что данные пользователя были в localStorage.

#### Причина
Проблема была в том, что при запросах к API сервер не мог правильно проверить авторизацию пользователя. Возможные причины:
- Заголовки авторизации не отправлялись правильно
- Сервер не мог найти пользователя в `users.json` по логину
- Ошибка при чтении/парсинге `users.json`

#### Решение
**В `server.js`:**
- ✅ Добавлено подробное логирование в функцию `requireAuth`:
  - Логирование всех входящих заголовков
  - Логирование данных из body запроса
  - Логирование процесса поиска пользователя
  - Вывод всех доступных логинов при ошибке поиска
  - Подтверждение успешного нахождения пользователя

**В `admin.html`:**
- ✅ Добавлено подробное логирование в функцию `getAuthHeaders()`:
  - Проверка наличия данных в localStorage
  - Вывод распарсенных данных пользователя
  - Подтверждение добавления каждого заголовка
  - Вывод итоговых заголовков для отправки

Теперь при проблемах с авторизацией в логах сервера и консоли браузера будет видна полная картина того, что происходит на каждом этапе проверки авторизации.

### Исправление ошибки "Cannot read properties of undefined (reading 'userLogin')" (2025-01-XX)

#### Проблема
После очистки localStorage и перезагрузки сервера возникала ошибка 500 "Ошибка проверки авторизации" с деталями: `"Cannot read properties of undefined (reading 'userLogin')"` при запросах к:
- `GET /api/users` - загрузка списка пользователей
- `GET /api/activity-logs` - загрузка логов активности

#### Причина
В функциях `requireAuth` и `optionalAuth` происходило обращение к `req.body.userLogin` и `req.body.userName` без проверки существования `req.body`. Для GET-запросов `req.body` может быть `undefined`, что вызывало ошибку при попытке прочитать свойство `userLogin` из `undefined`.

#### Решение
**В `server.js`:**
- ✅ Добавлена проверка существования `req.body` перед обращением к его свойствам
- ✅ Использование безопасного доступа: `(req.body && req.body.userLogin)` вместо `req.body.userLogin`
- ✅ Исправлено в функции `requireAuth` (строки 229, 233)
- ✅ Исправлено в функции `optionalAuth` (строки 371, 374)
- ✅ Улучшено логирование с использованием optional chaining для безопасности

**Технические детали:**
- Для GET-запросов `req.body` может быть `undefined`, поэтому необходима проверка
- Для POST/PUT-запросов `req.body` всегда существует (благодаря `express.json()` middleware)
- Заголовки авторизации (`X-User-Login`, `X-User-Name`) работают для всех типов запросов

#### Результат
- ✅ GET-запросы теперь корректно обрабатываются без ошибок
- ✅ Авторизация работает для всех типов HTTP-запросов
- ✅ Улучшена устойчивость кода к различным типам запросов

---

**Важно:** XSS уязвимости критичны для безопасности. Все места с пользовательскими данными должны быть исправлены перед деплоем в продакшен.

