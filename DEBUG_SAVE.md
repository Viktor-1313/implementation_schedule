# 🔍 Диагностика проблемы с сохранением данных

## Проблема
Даты не сохраняются даже локально после изменения.

## Как проверить работу сохранения

### 1. Откройте консоль браузера (F12)

### 2. Измените дату любой задачи

### 3. Проверьте сообщения в консоли:

**Должны появиться:**
- `📝 Изменение задачи: startDate для задачи: [название]`
- `⏱️ Запланировано автоматическое сохранение через 2 секунды`
- `⏰ Таймер сработал, начинаем сохранение...`
- `💾 Сохранение графика для компании: [название]`
- `📦 Размер данных: [число] байт`
- `✅ Полное состояние графика сохранено успешно`

### 4. Если видите ошибки:

**Ошибка: "⚠️ Компания не указана"**
- Проверьте URL: должен быть `?company=название`
- Или проверьте localStorage: `localStorage.getItem('gantt-company')`

**Ошибка: "⚠️ Режим просмотра"**
- Вы в режиме просмотра, а не редактирования
- Войдите в систему и откройте график в режиме редактирования

**Ошибка: "❌ Ошибка сохранения"**
- Проверьте вкладку Network в DevTools
- Найдите запрос `/api/gantt-state`
- Проверьте статус ответа (должен быть 200)

### 5. Проверьте серверные логи (локально):

В терминале, где запущен сервер, должны появиться:
- `💾 Сохранение графика для компании: [название]`
- `📁 Путь к файлу: [путь]`
- `📦 Размер данных: [число] байт`
- `✅ График успешно сохранен в файл: [путь]`

### 6. Проверьте файл данных:

После сохранения проверьте, создался ли файл:
- `gantt-state-[название-компании].json` в папке с проектом
- Откройте файл и проверьте, что там есть ваши изменения

## Возможные проблемы и решения

### Проблема 1: Компания не определяется

**Симптомы:** В консоли `⚠️ Компания не указана`

**Решение:**
1. Убедитесь, что в URL есть `?company=название`
2. Или проверьте localStorage в консоли:
   ```javascript
   localStorage.getItem('gantt-company')
   ```

### Проблема 2: Режим просмотра

**Симптомы:** В консоли `⚠️ Режим просмотра`

**Решение:**
1. Войдите в систему (не используйте "Режим просмотра")
2. Откройте график компании

### Проблема 3: Данные не отправляются

**Симптомы:** Нет запроса в Network или ошибка сети

**Решение:**
1. Проверьте, что сервер запущен
2. Проверьте URL сервера (должен быть правильный)
3. Проверьте CORS настройки (если сервер на другом домене)

### Проблема 4: Сервер не сохраняет

**Симптомы:** Запрос отправлен, но файл не создается

**Решение:**
1. Проверьте права на запись в папку проекта
2. Проверьте логи сервера на ошибки
3. Убедитесь, что путь к файлу правильный

## Быстрая проверка в консоли

Выполните в консоли браузера:

```javascript
// Проверить текущую компанию
console.log('Компания:', localStorage.getItem('gantt-company'));

// Проверить режим
console.log('Режим:', localStorage.getItem('gantt-mode'));

// Принудительно сохранить
saveFullGanttState();

// Проверить данные задач
console.log('Задачи:', tasks.length);
console.log('Первая задача:', tasks[0]);
```

## Что было исправлено

1. ✅ Добавлено подробное логирование всех этапов сохранения
2. ✅ Добавлена проверка компании и режима перед сохранением
3. ✅ Улучшено сохранение при уходе со страницы (keepalive)
4. ✅ Добавлены предупреждения при пропуске сохранения

## Следующие шаги

1. Откройте консоль браузера
2. Измените дату задачи
3. Подождите 2 секунды
4. Проверьте сообщения в консоли
5. Сообщите, какие сообщения вы видите

Это поможет точно определить, где проблема.









