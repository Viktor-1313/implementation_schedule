# ОТЧЕТ О ПРОВЕРКЕ БЕЗОПАСНОСТИ

**Дата проверки:** 2025-01-XX  
**Проверенные компоненты:**
- Компании (`companies.html`, `server.js` - endpoints компаний)
- График Ганта (`implementation_schedule.html`, `server.js` - endpoints графика)
- Админка (`admin.html`, `server.js` - endpoints админки)
- Архив (`server.js` - endpoints архива)

---

## 🔴 КРИТИЧЕСКИЕ УЯЗВИМОСТИ

### 1. Path Traversal в endpoints работы с компаниями

**Файл:** `server.js`  
**Строки:** 1050, 1158, 1255, и другие места, где используется `companyId` из `req.params.id` или `req.query.company`

**Проблема:**
В некоторых endpoints `companyId` используется для построения путей к файлам БЕЗ предварительной валидации через `validateCompanyId()` или `isValidCompanyId()`.

**Пример уязвимого кода (строка 1050):**
```javascript
let companyId = req.params.id;
// ... декодирование ...
const dataFile = getCompanyDataFile(companyId);  // ❌ НЕТ валидации!
const infoFile = getCompanyInfoFile(companyId);  // ❌ НЕТ валидации!
```

**Потенциальная атака:**
```javascript
// Злоумышленник может передать:
DELETE /api/companies/../../../etc/passwd

// Или:
GET /api/gantt-state?company=../../../../etc/passwd
```

**Исправление:**
Добавить валидацию `companyId` ПЕРЕД использованием в функциях `getCompanyDataFile()`, `getCompanyInfoFile()`, `getVersionsDir()`:

```javascript
let companyId = req.params.id;
try {
  companyId = decodeURIComponent(companyId);
} catch (decodeError) {
  return res.status(400).json({ ok: false, error: 'Некорректный ID компании' });
}

// ✅ ДОБАВИТЬ ВАЛИДАЦИЮ:
const validation = validateCompanyId(companyId);
if (!validation.valid) {
  return res.status(400).json({ ok: false, error: validation.error });
}
companyId = validation.value;

// Теперь безопасно использовать:
const dataFile = getCompanyDataFile(companyId);
```

**Затронутые endpoints:**
- `DELETE /api/companies/:id` (строка ~1050)
- `POST /api/companies/:id/archive` (строка ~1158)
- `GET /api/companies/:id/restore` (строка ~1255)
- `GET /api/gantt-state?company=...` (строка ~1387)
- `POST /api/gantt-state?company=...` (строка ~1407)
- Все endpoints работы с версиями (`/api/versions/...`)

---

## 🟡 СРЕДНИЕ УЯЗВИМОСТИ

### 2. Недостаточная защита от XSS в некоторых местах

**Файл:** `implementation_schedule.html`  
**Строки:** 13377, 11014, 11028

**Проблема:**
Используется `innerHTML` для вставки пользовательских данных, хотя есть частичное экранирование.

**Пример:**
```javascript
// Строка 13377:
row.innerHTML = `<td class="task-number-cell">...</td>`;

// Значения вставляются через input.value, что безопасно,
// но лучше использовать textContent для текстовых значений
```

**Рекомендация:**
Использовать `textContent` вместо `innerHTML` везде, где возможно, или убедиться, что все пользовательские данные экранируются через `escapeHtml()`.

---

### 3. Отсутствие CSRF токенов

**Файл:** `server.js`  
**Проблема:**  
Нет защиты от Cross-Site Request Forgery (CSRF) атак. Любой сайт может отправить запросы от имени авторизованного пользователя.

**Рекомендация:**
Добавить CSRF токены:
1. Генерировать токен при авторизации
2. Сохранять токен в сессии/куках
3. Требовать токен во всех POST/PUT/DELETE запросах
4. Проверять токен на сервере

**Библиотеки:**
- `csurf` для Express.js
- Или собственная реализация с проверкой Origin/Referer заголовков

---

### 4. Небезопасная работа с JSON десериализацией

**Файл:** `server.js`  
**Функция:** `safeJsonParse()` (нужно проверить реализацию)

**Проблема:**
Использование `JSON.parse()` на непроверенных данных может привести к DoS атак через:
- Глубоко вложенные объекты (prototype pollution)
- Очень большие JSON файлы

**Рекомендация:**
1. Ограничить максимальный размер JSON (уже есть лимит 10MB для запросов)
2. Ограничить глубину вложенности
3. Использовать `JSON.parse()` с reviver для проверки

---

## 🟢 НИЗКИЕ РИСКИ / РЕКОМЕНДАЦИИ

### 5. Rate Limiting

**Статус:** ✅ Реализовано  
**Файл:** `server.js`, строки 7-60

Rate limiting уже реализован, что хорошо защищает от brute-force атак и DoS.

---

### 6. Экранирование HTML

**Статус:** ✅ Частично реализовано  
**Файлы:** `companies.html`, `admin.html`

В этих файлах используется функция `escapeHtml()` для экранирования пользовательских данных перед вставкой в `innerHTML`. Это хорошо!

**Рекомендация:**  
Проверить `implementation_schedule.html` на использование `escapeHtml()` во всех местах с `innerHTML`.

---

### 7. Валидация входных данных

**Статус:** ✅ Частично реализовано  
**Файл:** `server.js`, строки 299-799

Есть функции валидации:
- `validateCompanyId()`
- `validateLogin()`
- `validatePassword()`
- `validateRole()`

**Проблема:**  
Валидация не используется во ВСЕХ местах, где нужно.

**Рекомендация:**  
Добавить валидацию во все endpoints, которые принимают пользовательский ввод.

---

### 8. Хранение паролей

**Статус:** ✅ Используется bcrypt  
**Файл:** `server.js`

Пароли хешируются через `bcrypt`, что правильно.

---

## 📋 ПЛАН ИСПРАВЛЕНИЙ (приоритет)

### Критично (исправить немедленно):

1. **Добавить валидацию `companyId` во все endpoints работы с файлами**
   - Файл: `server.js`
   - Метод: Добавить вызов `validateCompanyId()` перед использованием `getCompanyDataFile()`, `getCompanyInfoFile()`, `getVersionsDir()`
   - Затронутые endpoints: см. список выше

### Важно (исправить в ближайшее время):

2. **Добавить CSRF защиту**
   - Файл: `server.js`
   - Библиотека: `csurf` или собственная реализация

3. **Улучшить защиту от XSS**
   - Файл: `implementation_schedule.html`
   - Использовать `textContent` вместо `innerHTML` везде, где возможно

### Рекомендуется:

4. **Улучшить валидацию JSON**
   - Файл: `server.js`
   - Добавить проверку глубины вложенности

5. **Добавить Content Security Policy (CSP)**
   - Файлы: `*.html`
   - Заголовок: `Content-Security-Policy`

---

## ✅ ЧТО УЖЕ ХОРОШО РЕАЛИЗОВАНО

1. ✅ Rate limiting для защиты от brute-force
2. ✅ Хеширование паролей через bcrypt
3. ✅ Экранирование HTML в `companies.html` и `admin.html`
4. ✅ Функции валидации входных данных
5. ✅ Авторизация и проверка доступа к компаниям
6. ✅ Логирование действий пользователей

---

## 📝 ЗАМЕТКИ

- Функция `path.join()` в Node.js частично защищает от Path Traversal, но только если используется правильно
- Валидация `companyId` через регулярное выражение `/^[a-zA-Z0-9_-]+$/` эффективна против Path Traversal
- Использование `__dirname` в `path.join()` безопасно, но нужно валидировать входные данные

---

## 🔗 ДОПОЛНИТЕЛЬНЫЕ РЕСУРСЫ

- [OWASP Top 10](https://owasp.org/www-project-top-ten/)
- [Node.js Security Best Practices](https://nodejs.org/en/docs/guides/security/)
- [Express Security Best Practices](https://expressjs.com/en/advanced/best-practice-security.html)
