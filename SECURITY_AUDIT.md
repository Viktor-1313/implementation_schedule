# Аудит безопасности — Диаграмма Ганта ICONA

**Дата проверки:** 2025-01-XX  
**Версия:** 1.0

## 🔴 Критические уязвимости

### 1. Отсутствие проверки авторизации на API эндпоинтах

**Проблема:** Большинство API эндпоинтов не проверяют, авторизован ли пользователь перед выполнением операций.

**Затронутые эндпоинты:**
- `GET /api/companies` — получение списка компаний
- `POST /api/companies` — создание компании
- `PUT /api/companies/:id` — изменение компании
- `DELETE /api/companies/:id` — удаление компании
- `GET /api/gantt-state` — получение графика
- `POST /api/gantt-state` — сохранение графика
- `POST /api/company-info` — сохранение информации о компании
- `GET /api/users` — получение списка пользователей
- `POST /api/users` — создание пользователя
- `DELETE /api/users/:login` — удаление пользователя
- `PUT /api/users/:login` — изменение пользователя
- `GET /api/activity-logs` — получение логов
- `DELETE /api/activity-logs` — очистка логов

**Риск:** Неавторизованные пользователи могут:
- Просматривать данные всех компаний
- Создавать/удалять компании
- Изменять графики
- Управлять пользователями
- Просматривать логи активности

**Решение:**
```javascript
// Создать middleware для проверки авторизации
function requireAuth(req, res, next) {
  const authHeader = req.headers['authorization'];
  const token = authHeader && authHeader.split(' ')[1];
  
  if (!token) {
    return res.status(401).json({ ok: false, error: 'Требуется авторизация' });
  }
  
  // Проверка токена (JWT или сессия)
  // ...
  next();
}

// Применить к защищенным эндпоинтам
app.get('/api/companies', requireAuth, (req, res) => {
  // ...
});
```

### 2. Отсутствие проверки прав доступа

**Проблема:** Даже если пользователь авторизован, нет проверки, имеет ли он право на выполнение операции.

**Примеры:**
- Обычный пользователь может удалять компании
- Пользователь может изменять графики чужих компаний
- Пользователь может изменять других пользователей

**Решение:**
```javascript
function requireAdmin(req, res, next) {
  const user = req.user; // Из middleware авторизации
  if (user.role !== 'admin') {
    return res.status(403).json({ ok: false, error: 'Требуются права администратора' });
  }
  next();
}

function checkCompanyAccess(req, res, next) {
  const user = req.user;
  const companyId = req.query.company || req.params.id || req.body.company;
  
  if (user.role === 'admin') {
    return next(); // Админы имеют доступ ко всем компаниям
  }
  
  if (!user.companies || !user.companies.includes(companyId)) {
    return res.status(403).json({ ok: false, error: 'Нет доступа к этой компании' });
  }
  
  next();
}
```

### 3. CORS открыт для всех доменов

**Проблема:** `app.use(cors())` разрешает запросы с любых доменов.

**Риск:** Любой сайт может делать запросы к вашему API (CSRF атаки).

**Решение:**
```javascript
const cors = require('cors');

const corsOptions = {
  origin: function (origin, callback) {
    // Разрешенные домены
    const allowedOrigins = [
      'https://ваш-проект.onrender.com',
      'http://icona_academy.corppn.ru:3001',
      'http://localhost:3001'
    ];
    
    // Разрешаем запросы без origin (например, Postman)
    if (!origin || allowedOrigins.indexOf(origin) !== -1) {
      callback(null, true);
    } else {
      callback(new Error('Не разрешено политикой CORS'));
    }
  },
  credentials: true,
  optionsSuccessStatus: 200
};

app.use(cors(corsOptions));
```

### 4. XSS уязвимости (Cross-Site Scripting)

**Проблема:** Использование `innerHTML` без санитизации данных.

**Затронутые места:**
- `implementation_schedule.html` — множественные использования `innerHTML`
- `companies.html` — использование `innerHTML` для отображения данных компаний
- `admin.html` — использование `innerHTML` для отображения пользователей

**Пример уязвимого кода:**
```javascript
container.innerHTML = `<div>${company.name}</div>`;
// Если company.name содержит <script>alert('XSS')</script>, он выполнится
```

**Решение:**
```javascript
// Использовать textContent вместо innerHTML
container.textContent = company.name;

// Или использовать библиотеку для санитизации
const DOMPurify = require('dompurify');
container.innerHTML = DOMPurify.sanitize(company.name);

// Или использовать template literals с экранированием
function escapeHtml(text) {
  const div = document.createElement('div');
  div.textContent = text;
  return div.innerHTML;
}
container.innerHTML = `<div>${escapeHtml(company.name)}</div>`;
```

### 5. Хранение чувствительных данных в localStorage

**Проблема:** Данные пользователя хранятся в `localStorage`, что делает их уязвимыми для XSS атак.

**Риск:** Если злоумышленник внедрит XSS, он может получить доступ к данным пользователя из localStorage.

**Решение:**
- Использовать `httpOnly` cookies для хранения токенов авторизации
- Минимизировать данные в localStorage
- Использовать `sessionStorage` вместо `localStorage` для временных данных
- Реализовать JWT токены с коротким временем жизни

## 🟠 Высокий приоритет

### 6. Отсутствие rate limiting

**Проблема:** Нет ограничения на количество запросов от одного IP/пользователя.

**Риск:** 
- DDoS атаки
- Брутфорс паролей
- Перегрузка сервера

**Решение:**
```javascript
const rateLimit = require('express-rate-limit');

// Общий лимит
const limiter = rateLimit({
  windowMs: 15 * 60 * 1000, // 15 минут
  max: 100 // максимум 100 запросов
});

// Строгий лимит для авторизации
const authLimiter = rateLimit({
  windowMs: 15 * 60 * 1000,
  max: 5, // максимум 5 попыток входа
  message: 'Слишком много попыток входа, попробуйте позже'
});

app.use('/api/', limiter);
app.use('/api/auth', authLimiter);
```

### 7. Отсутствие защиты от CSRF

**Проблема:** Нет защиты от Cross-Site Request Forgery атак.

**Риск:** Злоумышленник может заставить авторизованного пользователя выполнить действия от его имени.

**Решение:**
```javascript
const csrf = require('csurf');
const csrfProtection = csrf({ cookie: true });

// Применить к POST/PUT/DELETE запросам
app.post('/api/companies', csrfProtection, (req, res) => {
  // ...
});
```

### 8. Отсутствие валидации и санитизации входных данных

**Проблема:** Входные данные не проверяются на корректность и не очищаются от потенциально опасного контента.

**Примеры:**
- Нет проверки длины полей
- Нет проверки типов данных
- Нет санитизации строк

**Решение:**
```javascript
const validator = require('validator');
const { body, validationResult } = require('express-validator');

app.post('/api/companies', [
  body('id').trim().isLength({ min: 1, max: 100 }).matches(/^[a-zA-Z0-9_-]+$/),
  body('name').trim().isLength({ min: 1, max: 200 }).escape(),
], (req, res) => {
  const errors = validationResult(req);
  if (!errors.isEmpty()) {
    return res.status(400).json({ ok: false, errors: errors.array() });
  }
  // ...
});
```

### 9. Отсутствие HTTPS enforcement

**Проблема:** Нет принудительного использования HTTPS в продакшене.

**Риск:** Перехват данных при передаче по незащищенному соединению.

**Решение:**
```javascript
// Middleware для принудительного HTTPS
if (process.env.NODE_ENV === 'production') {
  app.use((req, res, next) => {
    if (req.header('x-forwarded-proto') !== 'https') {
      res.redirect(`https://${req.header('host')}${req.url}`);
    } else {
      next();
    }
  });
}

// Установить заголовки безопасности
app.use((req, res, next) => {
  res.setHeader('Strict-Transport-Security', 'max-age=31536000; includeSubDomains');
  res.setHeader('X-Content-Type-Options', 'nosniff');
  res.setHeader('X-Frame-Options', 'DENY');
  res.setHeader('X-XSS-Protection', '1; mode=block');
  res.setHeader('Content-Security-Policy', "default-src 'self'");
  next();
});
```

### 10. Path Traversal уязвимости

**Проблема:** Хотя есть валидация ID компании, нужно проверить все места, где используются пути к файлам.

**Решение:**
```javascript
// Улучшить валидацию
function isValidCompanyId(companyId) {
  if (!companyId || typeof companyId !== 'string') return false;
  // Проверка на path traversal
  if (companyId.includes('..') || companyId.includes('/') || companyId.includes('\\')) {
    return false;
  }
  return /^[a-zA-Z0-9_-]+$/.test(companyId);
}

// Нормализация путей
const path = require('path');
function getCompanyDataFile(companyId) {
  if (!isValidCompanyId(companyId)) {
    throw new Error('Неверный ID компании');
  }
  const filePath = path.join(__dirname, `gantt-state-${companyId}.json`);
  // Проверка, что путь не выходит за пределы директории
  const resolvedPath = path.resolve(filePath);
  const basePath = path.resolve(__dirname);
  if (!resolvedPath.startsWith(basePath)) {
    throw new Error('Path traversal detected');
  }
  return resolvedPath;
}
```

## 🟡 Средний приоритет

### 11. Отсутствие логирования безопасности

**Проблема:** Нет логирования подозрительной активности.

**Решение:**
```javascript
function logSecurityEvent(event, details, req) {
  const log = {
    timestamp: new Date().toISOString(),
    event: event,
    ip: req.ip || req.connection.remoteAddress,
    userAgent: req.get('user-agent'),
    details: details
  };
  // Записать в отдельный файл логов безопасности
  fs.appendFileSync('security.log', JSON.stringify(log) + '\n');
}

// Логировать неудачные попытки входа
app.post('/api/auth', async (req, res) => {
  // ...
  if (!passwordMatch) {
    logSecurityEvent('failed_login', { login: trimmedLogin }, req);
    return res.status(401).json({ ok: false, error: 'Неверный логин или пароль' });
  }
  // ...
});
```

### 12. Отсутствие валидации размера файлов

**Проблема:** Хотя есть лимит 10MB, нет проверки размера до сохранения.

**Решение:**
```javascript
const multer = require('multer');
const upload = multer({
  limits: { fileSize: 10 * 1024 * 1024 }, // 10MB
  fileFilter: (req, file, cb) => {
    // Проверка типа файла
    if (file.mimetype.startsWith('image/')) {
      cb(null, true);
    } else {
      cb(new Error('Только изображения разрешены'));
    }
  }
});
```

### 13. Отсутствие защиты от SQL Injection (хотя используется JSON)

**Примечание:** Используется файловая система, не SQL, но нужно быть осторожным с JSON парсингом.

**Решение:**
```javascript
// Всегда валидировать JSON перед парсингом
function safeJsonParse(jsonString) {
  try {
    const parsed = JSON.parse(jsonString);
    // Проверка на циклические ссылки
    JSON.stringify(parsed);
    return parsed;
  } catch (e) {
    throw new Error('Неверный формат JSON');
  }
}
```

### 14. Отсутствие защиты от NoSQL Injection

**Примечание:** Хотя используется JSON файлы, нужно валидировать все входные данные.

### 15. Отсутствие защиты от XXE (XML External Entity)

**Примечание:** Не используется XML, но если будет добавлен, нужно защититься.

## 🟢 Низкий приоритет (улучшения)

### 16. Улучшение обработки ошибок

**Проблема:** Детальные сообщения об ошибках могут раскрывать информацию о системе.

**Решение:**
```javascript
app.use((err, req, res, next) => {
  console.error('Ошибка:', err);
  
  // В продакшене не показывать детали ошибок
  const message = process.env.NODE_ENV === 'production' 
    ? 'Внутренняя ошибка сервера' 
    : err.message;
  
  res.status(err.status || 500).json({ 
    ok: false, 
    error: message 
  });
});
```

### 17. Улучшение паролей

**Рекомендации:**
- Требовать минимальную сложность пароля (минимум 8 символов, буквы, цифры)
- Реализовать политику смены паролей
- Добавить двухфакторную аутентификацию (2FA) для администраторов

### 18. Регулярное обновление зависимостей

**Рекомендация:** Использовать `npm audit` для проверки уязвимостей в зависимостях.

```bash
npm audit
npm audit fix
```

### 19. Использование Content Security Policy (CSP)

**Решение:**
```html
<meta http-equiv="Content-Security-Policy" content="
  default-src 'self';
  script-src 'self' 'unsafe-inline' 'unsafe-eval';
  style-src 'self' 'unsafe-inline';
  img-src 'self' data:;
  connect-src 'self';
">
```

### 20. Регулярные бэкапы

**Рекомендация:** Настроить автоматические бэкапы данных компаний и пользователей.

## 📋 План действий

### Немедленно (критично):
1. ✅ Добавить проверку авторизации на все API эндпоинты
2. ✅ Добавить проверку прав доступа
3. ✅ Настроить CORS правильно
4. ✅ Исправить XSS уязвимости (заменить innerHTML на безопасные методы)

### В течение недели:
5. ✅ Добавить rate limiting
6. ✅ Добавить CSRF защиту
7. ✅ Добавить валидацию входных данных
8. ✅ Принудительный HTTPS в продакшене

### В течение месяца:
9. ✅ Улучшить логирование безопасности
10. ✅ Реализовать JWT токены вместо localStorage
11. ✅ Добавить валидацию размера файлов
12. ✅ Улучшить обработку ошибок

## 📚 Рекомендуемые библиотеки

```json
{
  "dependencies": {
    "express-rate-limit": "^7.1.5",
    "csurf": "^1.11.0",
    "express-validator": "^7.0.1",
    "jsonwebtoken": "^9.0.2",
    "helmet": "^7.1.0",
    "express-session": "^1.18.0",
    "cookie-parser": "^1.4.6"
  }
}
```

## 🔗 Полезные ресурсы

- [OWASP Top 10](https://owasp.org/www-project-top-ten/)
- [Node.js Security Best Practices](https://nodejs.org/en/docs/guides/security/)
- [Express Security Best Practices](https://expressjs.com/en/advanced/best-practice-security.html)

---

**Важно:** Этот аудит не является исчерпывающим. Рекомендуется провести профессиональный аудит безопасности перед использованием в продакшене с критически важными данными.






